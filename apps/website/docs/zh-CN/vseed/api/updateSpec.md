# updateSpec

:::note{title=描述}
更新指定图表类型的 Spec Pipeline。
允许用户注入自定义的处理逻辑到最终 Spec 构建流程中。
:::

```ts
function updateSpec(chartType: ChartType, specPipe: SpecPipe): void
```

**Parameters:**

- 图表类型
- 新的 Spec Pipe

