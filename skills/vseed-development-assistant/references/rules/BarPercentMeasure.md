# BarPercentMeasure 约束

与 BarMeasure 规则相同：

- **禁止修改 ID**：measure 的 `id` 必须与 dataset 字段名一致
- **合法 encoding**：`xAxis`、`detail`、`color`、`label`、`tooltip`
- **禁止非法 encoding**：不能使用 `yAxis`、`angle`、`radius`、`size`
