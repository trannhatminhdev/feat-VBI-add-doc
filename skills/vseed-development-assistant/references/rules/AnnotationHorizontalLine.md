# AnnotationHorizontalLine 约束

## selector 规则

- **field 必须存在**：`field` 必须是 dataset 中实际存在的字段名
- **value 必须匹配**：
  - 离散字段：使用数据中存在的分类值
  - 数值/连续字段：使用合理的数值或范围

## 注解文本语言规则

生成 `text` 字段时：

1. 如用户明确指定语言，使用该语言
2. 否则检测 dataset 文本字段的主要语言并匹配
3. 语言检测不明确时，回退到用户输入语言，默认英语
