{
  "name": "or-group-product-category-comparison",
  "description": "办公用品与技术类品类对比：使用 OR 分组筛选两个品类，按区域对比销售额",
  "schema": [
    {
      "name": "area",
      "type": "string"
    },
    {
      "name": "product_type",
      "type": "string"
    },
    {
      "name": "sales",
      "type": "number"
    }
  ],
  "dsl": {
    "chartType": "column",
    "dimensions": [
      {
        "field": "area",
        "alias": "区域"
      }
    ],
    "measures": [
      {
        "field": "sales",
        "alias": "销售额",
        "encoding": "yAxis",
        "aggregate": {
          "func": "sum"
        }
      }
    ],
    "limit": 20,
    "whereFilter": {
      "op": "and",
      "conditions": []
    }
  },
  "code": "const applyBuilder = (builder: VBIBuilder) => {\n  builder.whereFilter.addGroup('or', group => {\n    group\n      .add('product_type', node => node.setOperator('eq').setValue('办公用品'))\n      .add('product_type', node => node.setOperator('eq').setValue('技术'));\n  });\n}"
}
