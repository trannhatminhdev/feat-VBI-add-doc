{
  "name": "bar-by-province-with-filter",
  "description": "柱状图 - 按省份销售额（带过滤条件）",
  "schema": [
    { "name": "province", "type": "string" },
    { "name": "sales", "type": "number" }
  ],
  "dsl": {
    "chartType": "bar",
    "dimensions": [{ "field": "province", "alias": "省份" }],
    "measures": [{ "field": "sales", "alias": "销售额", "encoding": "xAxis", "aggregate": { "func": "sum" } }],
    "filters": []
  },
  "code": "const applyBuilder = (builder: VBIBuilder) => { builder.whereFilters.addWhereFilter({ field: 'sales', operator: 'gt', value: 100000 }); }"
}
