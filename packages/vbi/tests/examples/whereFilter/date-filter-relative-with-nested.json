{
  "name": "date-filter-relative-with-nested-conditions",
  "description": "日期过滤与嵌套条件组合：筛选近30天内消费者或企业客户的高额订单，按省份统计销售额与利润",
  "schema": [
    { "name": "order_date", "type": "string" },
    { "name": "customer_type", "type": "string" },
    { "name": "province", "type": "string" },
    { "name": "sales", "type": "number" },
    { "name": "profit", "type": "number" }
  ],
  "dsl": {
    "chartType": "column",
    "dimensions": [{ "field": "province", "alias": "省份" }],
    "measures": [
      {
        "field": "sales",
        "alias": "销售额",
        "encoding": "yAxis",
        "aggregate": { "func": "sum" }
      },
      {
        "field": "profit",
        "alias": "利润",
        "encoding": "yAxis",
        "aggregate": { "func": "sum" }
      }
    ],
    "limit": 20,
    "whereFilter": { "id": "root", "op": "and", "conditions": [] }
  },
  "code": "const applyBuilder = (builder: VBIChartBuilder) => {\n  builder.whereFilter\n    .add('order_date', node => {\n      node.setDate({ type: 'relative', mode: 'last', amount: 30, unit: 'day' });\n    })\n    .add('sales', node => node.setOperator('>').setValue(500))\n    .addGroup('or', group => {\n      group\n        .add('customer_type', n => n.setOperator('eq').setValue('消费者'))\n        .add('customer_type', n => n.setOperator('in').setValue(['公司', '小型企业']));\n    });\n}"
}
