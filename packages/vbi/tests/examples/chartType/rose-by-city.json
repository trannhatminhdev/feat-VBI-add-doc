{
  "name": "rose-by-city",
  "description": "玫瑰图 - 按城市销售额",
  "schema": [
    { "name": "city", "type": "string" },
    { "name": "sales", "type": "number" }
  ],
  "dsl": {
    "chartType": "rose",
    "dimensions": [{ "field": "city", "alias": "城市" }],
    "measures": [{ "field": "sales", "alias": "销售额", "encoding": "radius", "aggregate": { "func": "sum" } }]
  },
  "code": "const applyBuilder = (builder: VBIBuilder) => { builder.chartType.changeChartType('pie'); }"
}
