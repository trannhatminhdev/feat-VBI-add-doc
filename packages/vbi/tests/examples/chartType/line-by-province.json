{
  "name": "line-by-province",
  "description": "折线图 - 按省份销售额趋势",
  "schema": [
    { "name": "province", "type": "string" },
    { "name": "sales", "type": "number" }
  ],
  "dsl": {
    "chartType": "line",
    "dimensions": [{ "field": "province", "alias": "省份" }],
    "measures": [{ "field": "sales", "alias": "销售额", "encoding": "yAxis", "aggregate": { "func": "sum" } }],
    "limit": 10
  },
  "code": "const applyBuilder = (builder: VBIChartBuilder) => { builder.chartType.changeChartType('area'); }"
}
