{
  "name": "update-date-dimension-month",
  "description": "将已有日期维度更新为按月聚合",
  "schema": [
    {
      "name": "order_date",
      "type": "string"
    },
    {
      "name": "sales",
      "type": "number"
    }
  ],
  "dsl": {
    "chartType": "table",
    "dimensions": [
      {
        "field": "order_date",
        "alias": "原订单日期"
      }
    ],
    "measures": [
      {
        "field": "sales",
        "alias": "销售额",
        "encoding": "yAxis",
        "aggregate": {
          "func": "sum"
        }
      }
    ],
    "limit": 20
  },
  "code": "const applyBuilder = (builder: VBIChartBuilder) => {\n  const dimensionId = builder.dimensions.toJSON().find(item => item.field === 'order_date')?.id\n  if (dimensionId) {\n    const dimension = builder.dimensions.find((node) => node.getId() === dimensionId)\n    if (dimension) {\n      dimension.setAlias('待调整的订单日期')\n    }\n    builder.dimensions.update(dimensionId, node => {\n      node.setAlias('月份').setAggregate({ func: 'toMonth' })\n    })\n  }\n}"
}
