{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "doc_build/**"]
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "doc_build/**"]
    }
  }
}
